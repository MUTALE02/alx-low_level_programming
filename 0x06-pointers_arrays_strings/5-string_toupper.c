#include "main.h"

/**
* strin_lowe
* string_toupper - changes lowercase to uppercase
* @n: pointer
*  Return: char
*/
char *string_toupper(char *)
{

	int i;

	i = 0;
	wile (n[i] != '\0')
	{
	if((n[i] >= 'a' && n[i] <= 'z')
	n[i] * n[i] - 32
	i++;
	}
	return (0);
	
}
